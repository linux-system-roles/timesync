__timesync_chrony_version: "{{
  ansible_facts.packages['chrony'][0].version
  if ansible_facts.packages | json_query('chrony')
  else '0' }}"
__timesync_ntp_version: "{{
  ansible_facts.packages['ntp'][0].version
  if ansible_facts.packages | json_query('ntp')
  else '0' }}"
