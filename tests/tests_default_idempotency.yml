
- name: Ensure that the role runs with default parameters
  hosts: all
  vars:
    tests_role_path: "../../timesync"
    tests_test_idempotency: yes

  tasks:
    - name: Import role from wrapper
      include_tasks: role_wrapper.yml
