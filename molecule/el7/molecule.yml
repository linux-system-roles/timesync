---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: vagrant
  provider:
    name: virtualbox
lint:
  name: yamllint
  options:
    config-file: .yamllint
platforms:
  - box: bento/centos-7.6
    groups:
      - redhat
      - current
    name: centos7
    provision: false
provisioner:
  lint:
    name: ansible-lint
  name: ansible
  options:
    limit: ${TEST_OS:-""}
  playbooks:
    cleanup: ${MOLECULE_CLEANUP_PLAYBOOK:-../resources/playbooks/cleanup.yml}
    converge: ${MOLECULE_PLAYBOOK:-../resources/playbooks/playbook.yml}
    create: ${MOLECULE_CREATE_PLAYBOOK:-../resources/playbooks/create.yml}
scenario:
  name: el7
  test_sequence:
    - lint
    - dependency
    - cleanup
    - destroy
    - syntax
    - create
    - prepare
    - converge
    - idempotence
    - side_effect
    - verify
    - cleanup
    - destroy
verifier:
  directory: ../resources/tests
  lint:
    name: flake8
  name: testinfra
